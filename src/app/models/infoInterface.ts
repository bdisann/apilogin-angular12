interface INFO_INTERFACE {
  acces_token: string;
  platform: string;
  query: string;
  analisis: string;
  size: number;
  page: number;
  order: string;
  sortBy: string;
}

export default INFO_INTERFACE;
